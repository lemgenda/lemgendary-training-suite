--extra-index-url https://aiinfra.pkgs.visualstudio.com/PublicPackages/_packaging/onnxruntime-cuda-12/pypi/simple/
# LemGendary AI Training Suite - 2026 SOTA Stack
# Python 3.12+ / PyTorch 2.4.0+

# --- Core Engine ---
torch>=2.4.0,<=2.5.1
torchvision>=0.19.0,<=0.20.1
torchaudio>=2.4.0,<=2.5.1
torchmetrics>=1.6.0
pillow>=10.3.0
numpy>=1.26.4,<2.2.0
pyyaml>=6.0.1
psutil>=5.9.8
pandas>=2.2.0,<2.3.0
pyarrow>=15.0.0
scipy>=1.12.0
tqdm>=4.66.2
opencv-python>=4.11.0
scikit-image>=0.22.0  # Not directly imported by training suite (SSIM is now GPU-native). Kept for tooling/Kaggle compatibility.
matplotlib>=3.8.0
einops>=0.7.0
timm>=0.9.0
requests

# --- SOTA Generative & Multimodal (Phase 4 & 5) ---
diffusers>=0.37.1
transformers>=5.7.0
accelerate>=0.35.0
peft>=0.14.0
bitsandbytes>=0.45.0
sentencepiece>=0.2.0
huggingface-hub>=0.26.0

# --- Detection & Safety (Phase 1 & 2) ---
ultralytics>=8.3.0
lpips>=0.1.4
pycocotools>=2.0.7
torch-fidelity>=0.3.0

# --- Deployment & Export ---
# Platform-Agnostic GPU Acceleration
# Linux (Kaggle): NVIDIA CUDA
onnxruntime-gpu>=1.20.0; sys_platform == 'linux' or sys_platform == 'linux2'
# Windows (Local): DirectML natively supports AMD, Intel, and NVIDIA GPUs
onnxruntime-directml>=1.20.0; sys_platform == 'win32'
onnx>=1.17.0
onnxsim>=0.4.36
onnxconverter-common>=1.14.0
onnxscript>=0.1.0

# --- Local AMD/Intel PyTorch Acceleration ---
# Provides GPU support for local training/inference on non-NVIDIA cards
# torch-directml>=0.2.0; sys_platform == 'win32'

# --- Data Acquisition ---
kaggle
kagglehub
gdown>=5.1.0
albumentations
websockets

# --- Forex Trading (MT5 Pipeline) ---
# MetaTrader5 Python API (Windows only — requires MT5 terminal installed)
MetaTrader5>=5.0.45; sys_platform == 'win32'
# Technical indicators (used by mt5_pipeline.py; falls back to manual NumPy if absent)
pandas-ta>=0.3.14b
