# ==============================================================================
# LemGendary Training Suite — Development / Local Requirements
# ==============================================================================
# Source of truth for local Windows/Linux/macOS development.
# On Kaggle/Colab, use requirements-training.txt (identical runtime deps,
# minus dev tooling).
#
# IMPORTANT: The --extra-index-url directive below lets pip resolve the CUDA
# wheels for torch/torchvision/torchaudio. Do NOT add a +cuXXX suffix to the
# pins themselves — that suffix makes the file non-installable on CPU hosts.
# The notebook generator already writes a --constraint file to force +cu118
# on P100 nodes; that constraint takes precedence over these version pins.
# ==============================================================================

--extra-index-url https://download.pytorch.org/whl/cu121

# ── Core Deep Learning ──────────────────────────────────────────────────────
torch>=2.4.0,<2.6.0
torchvision>=0.19.0,<0.21.0
torchaudio>=2.4.0,<2.6.0

# ── Scientific Stack ────────────────────────────────────────────────────────
# numpy<2.0 pin is intentional: torchmetrics[image] and pyarrow still have
# intermittent incompatibilities with numpy 2.x on Kaggle's Python 3.10.
numpy>=1.26.0,<2.0.0
scipy>=1.11.0,<1.15.0
pandas>=2.1.0,<2.3.0
pyarrow>=14.0.0,<18.0.0

# ── Metrics (LPIPS / FID / mAP / SSIM) ──────────────────────────────────────
# The [image] extra is MANDATORY — without it, LearnedPerceptualImagePatchSimilarity
# and FrechetInceptionDistance raise ImportError at runtime.
torchmetrics[image]>=1.4.0,<1.7.0
pycocotools>=2.0.7,<2.1.0

# ── Computer Vision ─────────────────────────────────────────────────────────
# opencv-python-headless is required on Kaggle/Colab: no libGL.so present,
# and the GUI build will fail to import even during headless training.
opencv-python-headless>=4.9.0.80,<4.11.0
scikit-image>=0.22.0,<0.25.0
albumentations>=1.4.0,<2.1.0
pillow>=10.2.0,<11.2.0

# ── Model Registry / Backbone helpers ───────────────────────────────────────
einops>=0.7.0,<0.9.0
timm>=0.9.16,<1.1.0
safetensors>=0.4.0,<0.6.0

# ── Diffusion / Text-to-Image (optional but retained for task_type dispatch) ─
diffusers>=0.27.0,<0.32.0
transformers>=4.40.0,<4.50.0
accelerate>=0.29.0,<1.4.0
peft>=0.10.0,<0.15.0
bitsandbytes>=0.43.0,<0.46.0
sentencepiece>=0.2.0,<0.3.0

# ── Object Detection ────────────────────────────────────────────────────────
# 8.4 moved model.add_callback to a different module path and breaks the
# core_loop.py SOTA early-stop hook.
ultralytics>=8.2.0,<8.4.0

# ── Cloud Hub / Federated Sync ──────────────────────────────────────────────
kaggle>=1.6.0,<2.0.0
kagglehub>=0.2.0,<0.4.0
huggingface-hub>=0.23.0,<0.28.0
gdown>=5.1.0,<5.3.0
websockets>=12.0,<15.0

# ── ONNX / Export ───────────────────────────────────────────────────────────
onnx>=1.16.0,<1.18.0
onnxsim>=0.4.36,<0.5.0
onnxconverter-common>=1.14.0,<1.17.0
onnxscript>=0.1.0,<0.3.0
onnxruntime-gpu>=1.18.0,<1.21.0; sys_platform == "linux"
onnxruntime-directml>=1.18.0,<1.21.0; sys_platform == "win32"

# ── Metrics: extra FID backend ──────────────────────────────────────────────
torch-fidelity>=0.3.0,<0.5.0

# ── Utility ─────────────────────────────────────────────────────────────────
pyyaml>=6.0.1,<6.1.0
psutil>=5.9.0,<7.1.0
tqdm>=4.66.0,<4.68.0
requests>=2.31.0,<2.33.0
matplotlib>=3.8.0,<3.11.0

# ── MT5 (Windows-only data pipeline) ────────────────────────────────────────
# Has no Linux/macOS wheel; will not install on Kaggle/Colab.
MetaTrader5>=5.0.45,<5.0.70; sys_platform == "win32"

# ── Forex indicators (fast path in data/mt5_pipeline.py) ────────────────────
pandas-ta-classic>=0.3.0,<0.9.0

# ==============================================================================
# Development / Type-Checking Tooling
# Safe to strip on Kaggle — see requirements-training.txt.
# ==============================================================================
pylint>=3.2.0,<3.4.0
pyright>=1.1.360,<1.1.420

# Type stubs (must match runtime library versions above).
types-PyYAML>=6.0.12
types-psutil>=5.9.5
types-requests>=2.31.0
types-tqdm>=4.66.0